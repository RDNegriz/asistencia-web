services:
  frontend:
    build: ./frontend
    container_name: laboratorio-frontend
    expose:
      - "80"
    networks:
      - laboratorio_net
    restart: unless-stopped

  backend:
    build: ./backend
    container_name: laboratorio-backend
    expose:
      - "3000"
    environment:
      - PORT=3000
      - DB_HOST=db
      - DB_USER=labo
      - DB_PASS=labo123
      - DB_NAME=laboratorio
    depends_on:
      - db
    networks:
      - laboratorio_net
    restart: unless-stopped

  proxy:
    image: nginx:latest
    container_name: laboratorio-proxy
    ports:
      - "8080:80"
    volumes:
      - ./nginx/proxy.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - frontend
      - backend
    networks:
      - laboratorio_net
    restart: unless-stopped

  db:
    image: mariadb:latest
    container_name: laboratorio-db
    environment:
      - MARIADB_ROOT_PASSWORD=1234
      - MARIADB_DATABASE=laboratorio
      - MARIADB_USER=labo
      - MARIADB_PASSWORD=labo123
    volumes:
      - ./db/data:/var/lib/mysql
    networks:
      - laboratorio_net
    restart: unless-stopped

networks:
  laboratorio_net:
    driver: bridge

